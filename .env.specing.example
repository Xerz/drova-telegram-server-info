# Primary Drova proxy token for live API sampling.
DROVA_PROXY_TOKEN=

# Station UUID that is safe for write-flow sampling.
TEST_STATION_UUID=

# Optional. Only needed if live Telegram UX checks are required.
TELEGRAM_BOT_TOKEN=

# Optional. Use when Drova or Telegram requests must go through a proxy.
HTTP_PROXY=
HTTPS_PROXY=
